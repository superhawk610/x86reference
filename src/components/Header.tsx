import React from 'react';
import { Link } from 'gatsby';

const Header = () => (
  <header>
    <h1 style={{ margin: 0 }}>
      <Link to="/">x86reference</Link>
    </h1>
  </header>
);

export default Header;
